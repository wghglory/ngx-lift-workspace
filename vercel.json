{
  "version": 2,
  "buildCommand": "npx nx run-many -t build --projects=ngx-lift,clr-lift --no-cloud && npx nx build demo --configuration=production --no-cloud",
  "outputDirectory": "dist/apps/demo/browser",
  "framework": null,
  "installCommand": "npm ci",
  "devCommand": "npx nx serve demo",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
